# Corrected blood age calculation code
# ... (other relevant parts of the code) 

# Assuming these are defined earlier in the code
sire_age_link = "[correct_link_for_sire]"
dam_age_link = "[correct_link_for_dam]"
sire_of_dam_age_link = "[correct_link_for_sire_of_dam]"

# Corrected age calculation using int() type conversion
sire_age = int(get_age(sire_age_link))
dam_age = int(get_age(dam_age_link))
sire_of_dam_age = int(get_age(sire_of_dam_age_link))

# Continue with the rest of the code
